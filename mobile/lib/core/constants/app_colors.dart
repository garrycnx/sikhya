import 'package:flutter/material.dart';

class AppColors {
  // Brand — enterprise deep blue
  static const primary        = Color(0xFF1565C0); // Blue 800
  static const primaryLight   = Color(0xFF1976D2); // Blue 700
  static const primaryDark    = Color(0xFF0D47A1); // Blue 900
  static const primarySurface = Color(0xFFE3F2FD); // Blue 50

  // Semantics
  static const success        = Color(0xFF16A34A);
  static const successSurface = Color(0xFFDCFCE7);
  static const warning        = Color(0xFFF59E0B);
  static const warningSurface = Color(0xFFFEF3C7);
  static const danger         = Color(0xFFDC2626);
  static const dangerSurface  = Color(0xFFFEE2E2);
  static const info           = Color(0xFF0284C7);
  static const infoSurface    = Color(0xFFE0F2FE);

  // Surfaces
  static const background     = Color(0xFFF4F6FB);
  static const surface        = Color(0xFFFFFFFF);
  static const surfaceAlt     = Color(0xFFF8FAFC);
  static const cardBorder     = Color(0xFFEEF2FF);

  // Text
  static const textPrimary    = Color(0xFF0F172A);
  static const textSecondary  = Color(0xFF64748B);
  static const textMuted      = Color(0xFF94A3B8);

  // Borders
  static const border         = Color(0xFFE2E8F0);

  // Feature colours (for cards/icons)
  static const attendance     = Color(0xFF0EA5E9);
  static const marks          = Color(0xFF16A34A);
  static const homework       = Color(0xFFF59E0B);
  static const timetable      = Color(0xFF8B5CF6);
  static const notifications  = Color(0xFF06B6D4);
  static const fees           = Color(0xFFDC2626);
}
